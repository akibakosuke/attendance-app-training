# スマートフォン出退勤打刻・課題完了報告アプリ 仕様書

## 1. 概要
スマートフォンからワンタップで出退勤を打刻し、管理者のLINEグループに自動通知するWebアプリケーションです。また、研修課題の完了報告機能も併記します。

## 2. 目的
1.  **打刻の簡便化**
    *   スマートフォンからワンタップで操作可能にすることで、日々の報告負荷を軽減する。
2.  **管理の効率化**
    *   LINEグループへのリアルタイム通知により、管理者が研修生の活動状況（出勤、退勤、課題進捗）を即座に把握できるようにする。
3.  **データ集約**
    *   Googleスプレッドシートに記録を一元管理し、勤怠管理や進捗確認を容易にする。

## 3. 画面構成

### 3.1 メイン画面
スマートフォンでの利用を想定したシンプルで直感的なUIとします。

*   **ヘッダー**
    *   アプリ名を表示
*   **ユーザー情報**
    *   現在ログインしている研修生ID・氏名を表示
*   **現在時刻**
    *   リアルタイムで日時を表示
*   **アクションボタンエリア**
    *   **出勤ボタン**: 未出勤状態で表示（大きく目立つデザイン）
    *   **退勤ボタン**: 出勤中に表示
    *   **課題完了報告ボタン**: 常時、または特定の条件下で表示
*   **ステータス表示**
    *   現在の状態（「出勤前」「勤務中」「退勤済」など）
*   **フッター**
    *   著作権表示など

### 3.2 課題完了報告モーダル/画面
*   アプリURL入力フォーム
*   コメント入力フォーム（任意）
*   送信ボタン
*   キャンセルボタン

## 4. 入出力仕様

### 4.1 入力
*   **ユーザー操作**
    1.  「出勤」ボタンタップ
    2.  「退勤」ボタンタップ
    3.  「課題完了報告」ボタンタップおよびフォーム入力（URL等）
*   **システム入力**
    1.  現在日時
    2.  位置情報（オプション：必要であれば）

### 4.2 出力
*   **画面表示**
    1.  ボタン押下時のローディング表示
    2.  処理完了メッセージ（ダイアログまたはトースト通知）
    3.  ボタン状態の更新
*   **外部連携**
    1.  Googleスプレッドシートへの行追加・更新
    2.  LINE Notify APIへのメッセージ送信

## 5. 機能一覧
1.  **出退勤打刻機能**
    *   出勤：開始時刻を記録し、LINE通知を行う。
    *   退勤：終了時刻を記録し、勤務時間を算出してLINE通知を行う。
2.  **入力バリデーション**
    *   出勤状態でなければ退勤できない制御など。
3.  **課題完了報告機能**
    *   作成したアプリのURLを入力し、完了報告をLINE通知およびシート記録する。
4.  **自動通知機能**
    *   操作内容に応じたフォーマットでLINEグループにメッセージを送信する。
5.  **データ記録機能**
    *   Google Sheets API (経由のGAS) を用いてデータを保存する。
6.  **PWA対応**
    *   スマホのホーム画面に追加可能にし、ネイティブアプリのように全画面で動作させる。

## 6. 使用技術
*   **フロントエンド**
    *   **HTML**: セマンティックなマークアップ
    *   **CSS**: レスポンシブデザイン、モダンなUI（Flexbox/Grid）、アニメーション
    *   **JavaScript**: Vanilla JS（フレームワークなし）、非同期通信（Fetch API）
    *   **PWA**: Web App Manifest, Service Worker
*   **バックエンド**
    *   **Google Apps Script (GAS)**: Web APIとして稼働
*   **データベース**
    *   **Google Spreadsheet**
*   **通知**
    *   **LINE Messaging API**
*   **ホスティング**
    *   **GitHub Pages**

## 7. データ構造

### 7.1 ローカルストレージ (Client-side)
ブラウザのリロード等に対応するため、最低限の状態を保持します。
*   `trainee_id`: 研修生ID
*   `trainee_name`: 研修生氏名
*   `current_status`: 現在のステータス (`clocked_in`, `clocked_out`, `none`)
*   `clock_in_time`: 出勤時刻（退勤時の計算用一時保持）

### 7.2 スプレッドシート構成

データは以下の3シートで管理します。各項目は列として横に並びます。

#### シート１：研修生マスタ
研修生の基本情報を管理するシート。

| A列: 研修生ID | B列: 氏名 | C列: ステータス |
| :--- | :--- | :--- |
| user01 | 田中太郎 | 在籍中 |
| user02 | 佐藤花子 | 修了 |

#### シート２：打刻記録
日々の出退勤データを蓄積するシート。

| A列: 日付 | B列: 研修生ID | C列: 氏名 | D列: 出勤時刻 | E列: 退勤時刻 | F列: 勤務時間 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 2025/10/05 | user01 | 田中太郎 | 09:00 | 18:00 | 8:00 |

#### シート３：課題完了記録
課題の提出状況を記録するシート。

| A列: 完了日時 | B列: 研修生ID | C列: 氏名 | D列: アプリURL | E列: 判定 |
| :--- | :--- | :--- | :--- | :--- |
| 2025/10/05 15:30 | user01 | 田中太郎 | https://... | OK |

## 8. 通知フォーマット例

### 8.1 出勤通知
```text
【出勤】
田中太郎
2025/10/05 09:05
```

### 8.2 退勤通知
```text
【退勤】
田中太郎
出勤：09:05
退勤：18:10
勤務：9時間5分
```

### 8.3 課題完了通知
```text
【🎉課題完了報告🎉】
研修生：田中太郎（user01）
完了：2025/10/05 15:30

アプリURL: https://your-github-username.github.io/attendance-app

確認をお願いします！
```

## 9. 処理フロー

### 9.1 出退勤打刻・課題完了報告時の処理フロー

1.  **📲 クライアント (PWA/JS)**
    *   ユーザーが「出勤」「退勤」「報告」ボタンをタップ。
    *   **処理**: 現在日時、研修生ID、操作種別（出勤/退勤/課題報告）をデータとして準備。

2.  **🌐 GAS (Web API)**
    *   クライアントからHTTP POSTリクエストを受信。
    *   **処理**:
        *   リクエストのバリデーション（例：出勤済みか、必要なデータが揃っているか）。
        *   打刻の場合、勤務時間計算（退勤時）。

3.  **📊 Google スプレッドシート**
    *   GASからデータ書き込みリクエストを受信。
    *   **処理**:
        *   操作種別に応じて、**シート２（打刻記録）またはシート３（課題完了記録）**に行を追加/更新。

4.  **💬 LINE Messaging API**
    *   GASから通知メッセージとトークンを用いてリクエストを送信。
    *   **処理**: 管理者LINEグループへ、定められた通知フォーマットでメッセージが送信されます。

5.  **📲 クライアント (PWA/JS)**
    *   GASからの処理結果（成功/エラー）を受信。
    *   **処理**: 画面に処理完了メッセージを表示し、ボタンの状態を更新。

